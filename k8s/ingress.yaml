apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/tls-acme: 'true'
    ingress.kubernetes.io/ssl-redirect: "false"
    kubernetes.io/ingress.class: "nginx"
    certmanager.k8s.io/issuer: letsencrypt-prod
spec:
  tls: # 取得した証明書を使って通信させる
  - secretName: cert-manager-tls
    hosts:
    - _VAR_DEV_CONTEST_FQDN # ドメイン
    - _VAR_PROD_CONTEST_FQDN # ドメイン
    - _VAR_TRAINING_CONTEST_FQDN
  rules:
  - host: _VAR_PROD_CONTEST_FQDN
    http:
      paths:
        - path: /
          backend:
            serviceName: ui
            servicePort: 3000
        - path: /api
          backend:
            serviceName: api
            servicePort: 3000
  - host: _VAR_TRAINING_CONTEST_FQDN
    http:
      paths:
        - path: /
          backend:
            serviceName: ui
            servicePort: 3000
        - path: /api
          backend:
            serviceName: api
            servicePort: 3000
  - host: _VAR_DEV_CONTEST_FQDN
    http:
      paths:
        - path: /
          backend:
            serviceName: ui
            servicePort: 3000
        - path: /api
          backend:
            serviceName: api
            servicePort: 3000
