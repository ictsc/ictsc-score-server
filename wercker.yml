box: ruby
services:
  - redis

build:
  steps:
    - bundle-install
    - script:
        name: create databasea
        code: bundle exec rake db:create
    - script:
        name: migration database
        code: bundle exec rake db:migrate
